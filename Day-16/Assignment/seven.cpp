// Byte Addressable 
// Byte addressable memory means each byte in memory has its own unique address.

//Example:
// char uses 1 byte → has 1 address
// int uses 4 bytes → each of the 4 bytes has a separate address
// So, every single byte can be accessed individually.